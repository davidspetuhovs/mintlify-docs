---
title: "Settings"
description: "Configure your chatbot's name, company info, API keys, members, webhooks, and data import/export."
---

import ApiKeyWarning from "/snippets/api-key-warning.mdx";

The settings page is where you configure how your chatbot operates — its identity, company information, API credentials, team access, webhook integrations, and data management. Navigate to **Settings** from your chatbot's sidebar.

![Chatbot settings](/images/chatbot-settings.png)

<Tabs>
  <Tab title="General">
    The General tab manages your chatbot's identity and lifecycle.

    ## Chatbot name

    Edit the display name of your chatbot. This name appears in the sidebar, dashboard headers, and reports. Changes save immediately when you submit.

    ### Delete chatbot

    <Warning>
      Deleting a chatbot is irreversible. All conversations, messages, analysis data, workflows, and webhooks associated with this chatbot are permanently deleted.
    </Warning>

    Use this only when you are certain you no longer need the chatbot or any of its data.
  </Tab>

  <Tab title="Company Info">
    The Company Info tab stores your company or organization information. These details provide context that can be used across the platform.

    Fill in relevant fields about your company to help OpenBat better understand the domain and context of your chatbot's conversations.
  </Tab>

  <Tab title="API Keys">
    The API Keys tab manages the credentials your SDK uses to authenticate with OpenBat.

    ## API key

    Your API key authenticates SDK requests to OpenBat. The settings page displays only the key prefix (e.g., `ob_live_xxxx...`) — the full key is never shown again after creation.

    <ApiKeyWarning />

    ### Rotate key

    Click **Rotate Key** to generate a new API key.

    <Warning>
      Rotating your key immediately invalidates the old one. Any SDK integrations using the previous key will fail until you update them with the new key.
    </Warning>

    The new full key is displayed once after rotation. Copy it and update your integration before leaving the page.
  </Tab>

  <Tab title="Members">
    The Members tab controls which organization members can view and manage a specific chatbot.

    ![Members](/images/settings-access.png)

    ## Default behavior

    By default, all organization members have access to all chatbots in the org. No configuration is needed unless you want to restrict access.

    ## Restrict access

    You can limit a chatbot to specific members only. This is useful for:

    - **Client-facing chatbots** where only the relevant account team should have access
    - **Sensitive internal chatbots** with restricted visibility
    - **Team-specific chatbots** managed by different sub-teams within your org

    ## Manage members

    The Members tab shows all members who currently have access, including their name, email, and role. Use the **Add member** action to grant access to an org member who does not have it yet.
  </Tab>

  <Tab title="Webhooks">
    The Webhooks tab is where you define the webhook endpoints used by [workflows](/guides/workflows) as action targets.

    ![Webhooks](/images/settings-webhooks.png)

    ## Define a webhook

    Each webhook has the following properties:

    | Property | Description |
    |---|---|
    | Name | A label used to identify the webhook in the workflow editor |
    | URL | The endpoint OpenBat sends POST requests to |
    | Type | `slack` for Slack incoming webhooks, or `http` for generic HTTP endpoints |
    | Enabled | Toggle the webhook on or off without deleting it |

    ## Centralized configuration

    Webhooks defined here are available as selectable actions in all workflows for this chatbot. If a URL changes, update it once in settings and every workflow referencing that webhook picks up the change automatically.

    Configure your webhooks here before referencing them in a workflow. See [workflows](/guides/workflows) for how to use webhooks as actions.
  </Tab>

  <Tab title="Import/Export Data">
    The Import/Export Data tab allows you to import and export conversation data for your chatbot.

    ## Import data

    Upload conversation data to populate your chatbot with existing conversations. This is useful when migrating from another platform or loading historical data for analysis.

    ## Export data

    Export your chatbot's conversation data for backup, reporting, or external analysis purposes.
  </Tab>
</Tabs>

## Related settings

For metadata field management and analysis configuration, see the dedicated guides:

- [Analysis configuration](/guides/analysis-configuration) — configure metadata fields, intent, topic, flag, behavior, outcome, and quality settings
- [Translation](/guides/translation) — set up automatic language detection and translation for multilingual conversations
